# Puertos de Servicios
PORT_GATEWAY=8080
PORT_GATEWAY_PUBLIC=8080
PORT_USER_AUTH=8081

# Conexión Base de Datos
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=

# Nombre de las bases de datos
DB_USER_AUTH_NAME=

# Perfil
SPRING_PROFILES_ACTIVE=dev

# Liquibase
LIQUIBASE_ENABLED=false